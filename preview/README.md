# Generated previews

OpenSCAD PNG renders belong in this directory rather than the project root.
Run `../render_previews.sh` from here, or `./render_previews.sh` from the project
root, to regenerate the standard preview set.

PNG files are intentionally ignored by Git.
